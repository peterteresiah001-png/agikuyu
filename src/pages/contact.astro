---
import Layout from "../layouts/Layout.astro";
import { SITE } from "../data/site.js";
---

<Layout title="Contact">
  <section data-section="contact-hero">
    <img src="https://picsum.photos/seed/contact-hero/1400/450" alt="Contact us" width="1400" height="450" />
    <h1>Contact</h1>
  </section>

  <section data-section="contact-details">
    <div>
      <h3>Phone</h3>
      <p>{SITE.phone}</p>
    </div>
    <div>
      <h3>Email</h3>
      <p>{SITE.email}</p>
    </div>
    <div>
      <h3>Location</h3>
      <p>{SITE.location}</p>
    </div>
  </section>

  <section data-section="contact-form">
    <h2>Send Us a Message</h2>
    <form data-form="contact">
      <label>
        Name
        <input type="text" name="name" required />
      </label>
      <label>
        Email
        <input type="email" name="email" required />
      </label>
      <label>
        Message
        <textarea name="message" rows="4" required></textarea>
      </label>
      <button type="submit">Send via WhatsApp</button>
    </form>
  </section>
</Layout>

<script>
  import { whatsappLink } from "../data/site.js";

  const form = document.querySelector('form[data-form="contact"]');
  form?.addEventListener("submit", (e) => {
    e.preventDefault();
    const data = new FormData(form as HTMLFormElement);
    const name = data.get("name");
    const email = data.get("email");
    const message = data.get("message");

    const text = `Contact Form Message\nName: ${name}\nEmail: ${email}\nMessage: ${message}`;
    window.open(whatsappLink(text), "_blank");
  });
</script>
