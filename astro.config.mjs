import { defineConfig } from 'astro/config';

export default defineConfig({
  // No CSS/UI framework wired up on purpose — this build is functionality-only.
  // Add Tailwind / your design system before shipping to production.
});
